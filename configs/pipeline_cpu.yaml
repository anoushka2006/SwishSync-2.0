# CPU-only tier — the mandatory tier. No GPU required, ever.
detector:   { name: yolo,      backend: cpu, weights: models/hoop_ball_yolov8_baseline.pt }
tracker:    { name: legacy_shot_engine }
calibrator: { name: manual_4pt }
events:     [ shot ]
